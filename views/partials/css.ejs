<%if (process.env.MODE === 'production') { %>
    <link rel="stylesheet" href="build/css/<%= process.env.npm_package_name %>.min.css"/>
<% } else { %>
	<link rel="stylesheet" href="/css/counter-main.css"/>
<% } %>


